import{_ as t,o as a,c as i,a2 as r}from"./chunks/framework.BXzK3EA4.js";const u=JSON.parse('{"title":"Implementation findings","description":"","frontmatter":{},"headers":[],"relativePath":"development/compatibility-findings.md","filePath":"development/compatibility-findings.md"}'),n={name:"development/compatibility-findings.md"};function o(s,e,l,d,c,p){return a(),i("div",null,[...e[0]||(e[0]=[r('<h1 id="implementation-findings" tabindex="-1">Implementation findings <a class="header-anchor" href="#implementation-findings" aria-label="Permalink to &quot;Implementation findings&quot;">​</a></h1><p>Experiment and implementation date: September 15, 2026. The <a href="/evidence/release.json">release record</a> binds final results to the exact local artifact. MacOS arm64 remains an open qualification gate.</p><h2 id="one-native-implementation" tabindex="-1">One native implementation <a class="header-anchor" href="#one-native-implementation" aria-label="Permalink to &quot;One native implementation&quot;">​</a></h2><p>Kitty 0.38.1 and 0.48.2 load the shared Python package through a global watcher. Bash, Zsh, and Fish native directory reports and prompt/command markers supply the same input contract. No shell adapter is required for the qualified root shell profiles.</p><p>Immediate command-completion callbacks can precede the prompt-ready state and the final directory report. A 20 ms coalesced callback reads the settled report, then rechecks the active pane in the selected tab before and after asynchronous file work. Inactive-pane reports cannot write another pane’s selection. OS windows have independent state, including when they lack desktop focus.</p><p>Startup waits for the first reliable local report. Unknown schemes, remote hosts, malformed encoding, disappeared directories, and lost reports produce a bounded diagnostic and the applicable fallback. Native kitty URI paths preserve literal percent signs, question marks, and hashes; standard file URIs are percent-decoded.</p><p>Nested Bash did not inherit native reporting in the real fixture. Direct nested interactive shells and known unsupported contexts therefore use a latched fallback until explicit resume. Noninteractive subprocesses retain the last reliable shell report. Nushell’s native reports were observed in a separate PTY, but product graphical qualification is not claimed.</p><h2 id="embedded-python-and-bounded-work" tabindex="-1">Embedded Python and bounded work <a class="header-anchor" href="#embedded-python-and-bounded-work" aria-label="Permalink to &quot;Embedded Python and bounded work&quot;">​</a></h2><p>The package loads from its own path under a unique module name. It does not alter the process import path or generate bytecode inside the installed bundle. The runtime uses Python’s standard library; Node belongs to site development.</p><p>Filesystem work runs outside UI callbacks. Only one job is submitted to the worker, with the latest pending request retained per OS window/config action. A five-second timeout retains the running handle, stops further collectors, and rejects additional work until the reader finishes. A later event can recover. Closing, pausing, or losing ownership discards pending window work.</p><p>Native kitty timers snapshot raw callback pointers before dispatch. Cancelling a later timer inside that batch can free its callback too early; a minimal fixture reproduced a native crash without loading Kittyscape. The compatibility module keeps one stable dispatcher and cancels only its Python callback entry, allowing the native one-shot to expire safely. Event bursts retain one timer per OS window.</p><p>Kitty’s native loop keeps Python’s interpreter lock between callbacks, so the pending-work collector yields with <code>sleep(0)</code>. A successful image change posts a main-loop wakeup so the renderer paints it without another shell command. Removing an image from a transparent window also needs explicit screen damage. Kittyscape refreshes only that OS window’s active pane after a successful write, so an unfocused window repaints without requiring another command. The real pixel-based fixture measures 100 transitions; a separate 60-second idle fixture checks zero Kittyscape timers and image writes. See the release record for the measured values and hardware rather than inferring timing from mocks.</p><p>The image cache is limited to 64 entries and 32 MiB of encoded data. PNG loading validates container CRCs, encoding, dimensions, bounded decompression, scanline lengths, and filters before calling kitty’s decoder. Rule images are limited to 16 MiB, 4096 pixels per dimension, 16 million pixels, and 64 MiB decoded data.</p><h2 id="restoration-and-ownership" tabindex="-1">Restoration and ownership <a class="header-anchor" href="#restoration-and-ownership" aria-label="Permalink to &quot;Restoration and ownership&quot;">​</a></h2><p>Kitty exposes whether an OS window has an image, but no getter for its exact effective image bytes, selected list index, or layout override. Configured filenames are insufficient for arbitrary preexisting GPU state.</p><p>The watcher captures known startup state and observes successful kitty-side image writes in one compatibility module. It never changes shared defaults during a directory event. On current kitty, restoring an inherited baseline uses the recorded global image index, preserving kitty’s loaded image cache and removing the temporary layout override. Ordinary reloads retain the observed absolute index. The older runtime restores retained PNG bytes.</p><p>Successful external byte uploads provide a recoverable baseline and pause the affected OS window. Explicit resume is required. Path-only custom writes, oversized retained payloads, unknown existing windows, and relative index increments cannot supply a trustworthy baseline and remain paused. Direct C writers bypass the observation boundary and require manual coordination.</p><p>Normal option reloads and native dark/light theme changes have a separate suspension boundary. Theme image updates replace the inherited baseline; explicit manual pauses remain paused. Older kitty reloads preserve its existing image bytes while adopting new rendering settings. Layout overrides are preserved only where that kitty version implements them.</p><h2 id="qualification-tools-and-isolation" tabindex="-1">Qualification tools and isolation <a class="header-anchor" href="#qualification-tools-and-isolation" aria-label="Permalink to &quot;Qualification tools and isolation&quot;">​</a></h2><p>The graphical suite uses distinctive deterministic 1920×1080 PNG fixtures. X11 runs on an explicitly reserved Xvfb display. Wayland requires a separate Hyprland instance with a private runtime directory under <code>/tmp</code> and workspace 15 active inside that instance. The verifier refuses the daily compositor runtime and never switches the operator’s workspace. <code>grim -T</code> exports only the exact fixture toplevel.</p><p>Always reserve a known-unused test display explicitly. Refuse existing socket or lock paths and clean up only the test process that the runner started.</p><p>Bash 5.3.20 was built from signed GNU sources and patches in a temporary directory, after explicit permission. Older Bash/Zsh/Fish and portable kitty binaries were extracted into temporary directories; no host package or default shell was replaced. Exact identities accompany the release evidence.</p><h2 id="sources" tabindex="-1">Sources <a class="header-anchor" href="#sources" aria-label="Permalink to &quot;Sources&quot;">​</a></h2><ul><li><a href="https://sw.kovidgoyal.net/kitty/launch/#watching-launched-windows" target="_blank" rel="noreferrer">Watcher callbacks</a></li><li><a href="https://sw.kovidgoyal.net/kitty/shell-integration/" target="_blank" rel="noreferrer">Native shell integration</a></li><li><a href="https://github.com/kovidgoyal/kitty/tree/v0.38.1" target="_blank" rel="noreferrer">Kitty 0.38.1 source</a></li><li><a href="https://github.com/kovidgoyal/kitty/tree/v0.48.2" target="_blank" rel="noreferrer">Kitty 0.48.2 source</a></li><li><a href="https://www.w3.org/TR/png-3/" target="_blank" rel="noreferrer">PNG specification</a></li><li><a href="https://ftp.gnu.org/gnu/bash/" target="_blank" rel="noreferrer">GNU Bash sources and patches</a></li><li><a href="https://wayland.app/protocols/ext-foreign-toplevel-list-v1" target="_blank" rel="noreferrer">Standard foreign-toplevel list protocol</a></li></ul>',24)])])}const m=t(n,[["render",o]]);export{u as __pageData,m as default};
