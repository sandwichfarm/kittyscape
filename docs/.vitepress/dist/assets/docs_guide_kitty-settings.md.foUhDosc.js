import{_ as t,o as a,c as n,a2 as i}from"./chunks/framework.BXzK3EA4.js";const g=JSON.parse('{"title":"Kitty settings and restoration","description":"","frontmatter":{},"headers":[],"relativePath":"docs/guide/kitty-settings.md","filePath":"guide/kitty-settings.md"}'),s={name:"docs/guide/kitty-settings.md"};function o(r,e,d,l,h,c){return a(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="kitty-settings-and-restoration" tabindex="-1">Kitty settings and restoration <a class="header-anchor" href="#kitty-settings-and-restoration" aria-label="Permalink to &quot;Kitty settings and restoration&quot;">​</a></h1><p>Kittyscape selects an image and may select one central settings profile. Scoped profiles support OS-window font size and active-pane padding/margin. Process profiles use kitty&#39;s native configuration loader for a strict allowlist and affect the complete kitty process. See the <a href="./../reference/configuration.html#directory-profiles">configuration reference</a>.</p><h2 id="one-image-per-os-window" tabindex="-1">One image per OS window <a class="header-anchor" href="#one-image-per-os-window" aria-label="Permalink to &quot;One image per OS window&quot;">​</a></h2><p>A kitty <strong>pane</strong> is a terminal inside an OS window. Tabs contain panes, but the background belongs to the outer OS window:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>OS window A                         OS window B</span></span>
<span class="line"><span>└─ selected tab                      └─ selected tab</span></span>
<span class="line"><span>   ├─ active pane → image A             └─ active pane → image B</span></span>
<span class="line"><span>   └─ inactive pane → no override</span></span></code></pre></div><p>Changing focus re-evaluates the newly active pane. Background activity in an inactive pane or unselected tab must not override the visible selection. An unfocused OS window still owns independent state.</p><h2 id="what-stays-unchanged" tabindex="-1">What stays unchanged <a class="header-anchor" href="#what-stays-unchanged" aria-label="Permalink to &quot;What stays unchanged&quot;">​</a></h2><p>Runtime switching preserves effective rendering settings and persistent configuration files in the qualified profiles. It does not set the default image for future windows. Repeating an unchanged image and rendering context adds no image upload.</p><p>These are measured runtime behaviors. The <a href="./../development/compatibility-findings.html">findings</a> identify the profiles and versions for which preservation has been verified.</p><h2 id="restoring-the-baseline" tabindex="-1">Restoring the baseline <a class="header-anchor" href="#restoring-the-baseline" aria-label="Permalink to &quot;Restoring the baseline&quot;">​</a></h2><p>The baseline is the effective original image for an OS window, captured before Kittyscape changes it. No image and a single image are different baseline states. Leaving a rule, disabling the extension, or restoring it uses that baseline while Kittyscape still owns the window. The watcher must observe the window’s startup; see <a href="./installation.html#existing-windows">existing windows</a>.</p><p>Removing the current image is correct only when the baseline contained no image. A configured image list, runtime-selected image, theme change, or per-window override may need additional baseline information. Such profiles are qualified separately; see <a href="./../reference/compatibility.html">compatibility</a>.</p><h2 id="themes-reloads-and-other-writers" tabindex="-1">Themes, reloads, and other writers <a class="header-anchor" href="#themes-reloads-and-other-writers" aria-label="Permalink to &quot;Themes, reloads, and other writers&quot;">​</a></h2><p>Do not let two background tools contend over the same OS window. If another writer replaces an image, Kittyscape must relinquish ownership and avoid overwriting that change during restoration.</p><p>Scoped settings use live ownership checks. If another tool changes an owned font size, padding, or margin, Kittyscape does not restore over that value. Process profiles suspend scoped writes across all OS windows. The focused OS window controls process selection; other windows retain their desired scoped profiles until the process profile ends. Process reloads never rewrite the daily <code>kitty.conf</code> and never inherit unspecified values from the prior overlay.</p><p>Use <strong>Ctrl+Shift+F7</strong> to restore owned backgrounds and pause before manual changes. Review status with <strong>Ctrl+Shift+F6</strong>, then explicitly resume with <strong>Ctrl+Shift+F8</strong> only when the current baseline is known. An observed external write pauses only its affected OS window; an explicit pause affects the whole instance, including windows opened later. See the full <a href="./../reference/actions.html">action reference</a>.</p><p>Unknown baselines remain paused. Resume does not read back or guess unknown GPU image state. Current kitty’s dark/light theme changes use a dedicated boundary: owned images are restored before the native change, the new theme baseline is recorded, and previously unpaused windows resume. A manual pause survives the theme change. Ordinary reloads preserve observed absolute image-list indices.</p><p>On kitty 0.38.1, an ordinary reload retains the previously displayed original image bytes while rendering settings follow the reload, matching that version’s native behavior. The newer global image-index API is not assumed on older versions.</p><p>A path-only custom Python writer cannot provide the exact bytes kitty loaded. Its baseline remains unknown, even if that path currently exists. Use a fresh OS window with a known startup baseline, or an observed byte-based image upload through kitty’s own authorized control mechanism. Direct C writers and relative image-index changes remain outside concurrent automatic mode.</p><h2 id="recovery" tabindex="-1">Recovery <a class="header-anchor" href="#recovery" aria-label="Permalink to &quot;Recovery&quot;">​</a></h2><p>If switching or restoration looks wrong, pause automatic updates, inspect diagnostics, and preserve the working terminal. Follow <a href="./uninstall.html">uninstall and rollback</a> to remove only Kittyscape-owned setup. Do not change opacity or remove your normal theme to hide a restoration error.</p>`,21)])])}const u=t(s,[["render",o]]);export{g as __pageData,u as default};
