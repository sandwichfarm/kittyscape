# Superseded test layout

The private compositor placed both windows at the same position. Modern kitty stopped rendering the fully occluded window, so the screenshot assertion saw an old frame after native background state had restored. The final matrix uses tiled visible windows in a fresh output directory. These rows are not part of the final support matrix.
